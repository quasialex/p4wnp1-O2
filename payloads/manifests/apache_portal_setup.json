{
  "name": "apache_portal_setup",
  "group": "network",
  "summary": "Install & configure Apache for captive portal + payload hosting (optionally Let's Encrypt).",
  "requirements": ["net"],
  "harden": false,
  "preflight": [
    "apt-get update -y",
    "apt-get install -y apache2 certbot python3-certbot-apache",
    "a2enmod rewrite headers ssl proxy proxy_http"
  ],
  "script": "payloads/network/apache_portal_setup.py",
  "usage": [
    "p4wnctl.py payload run-now apache_portal_setup"
  ]
}
