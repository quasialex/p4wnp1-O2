{
  "name": "wifi_ap_captive_apache",
  "group": "network",
  "summary": "Bring up Wi‑Fi AP + DHCP + DNS spoof + Apache captive portal",
  "requirements": ["net"],
  "harden": false,
  "preflight": [
    "ip link set wlan0 down || true",
    "ip addr flush dev wlan0 || true",
    "ip link set wlan0 up || true",
    "sysctl -w net.ipv4.ip_forward=1"
  ],
  "script": "payloads/network/wifi_ap_captive_apache.py",
  "usage": [
    "p4wnctl.py payload run-now apache_portal_setup",
    "p4wnctl.py payload run-now wifi_ap_captive_apache"
  ]
}
